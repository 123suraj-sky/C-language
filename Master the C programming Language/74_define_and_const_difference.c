#include <stdio.h>

#define LIMIT1 5
const int LIMIT2  = 5;

// notes:
// we should use "define" as the whole program is replaces by its value
// in case of "const int" it occupies memory of int
// so "define" programs are much more efficient

int main()
{
    

    return 0;
}