#include <stdio.h>

int main()
{
    FILE *ptr;
    int s1, s2;  // s1 and s2 are salary of the employee
    char c1, c2; // c1 and c2 are name of the employee

    pritnf("Enter the name of the employee 1\n");
    scanf("%c", &c1);

    pritnf("Enter the name of the employee 2\n");
    scanf("%c", &c2);
    return 0;
}